using System.Collections;
using System.Collections.Generic;
using UnityEngine;


// add in code from ~16:00 in video

public class Testing : MonoBehaviour
{
    // Start is called before the first frame update
    private void Start()
    {
        MyGrid grid = new MyGrid(4, 2);
    }
}
